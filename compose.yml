services:
  frontend:
    build: 
      context: .
      dockerfile: ./dockerfiles/react/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./react-test-app:/app
      - /app/node_modules

  backend:
    build:
      context: .
      dockerfile: ./dockerfiles/springboot/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./demo:/app
      - ./demo/.gradle:/root/.gradle
    environment:
      - SPRING_DEVTOOLS_REMOTE_SECRET=secret
      - SPRING_OUTPUT_ANSI_ENABLED=ALWAYS
    # depends_on:
    #   - db
    restart: "no"

  backend-watcher:
    build:
      context: .
      dockerfile: ./dockerfiles/springboot/Dockerfile
    working_dir: /app
    command: ["bash", "-c", "chmod +x /app/gradlew && /app/gradlew --no-daemon classes --continuous"]
    volumes:
      - ./demo:/app
      - ./demo/.gradle-watcher:/app/.gradle
    restart: "no"
    depends_on:
      - backend